import time


class ImageAnalysisService:
def upload_image(self, image_filename: str)
    def uploadImage(self, image_filename: str):
        time.sleep(0.25)

    def detectTrafficLight(self, image_filename) -> bool:
        time.sleep(0.25)
        return True
